#!/usr/bin/env bash

# Define BPS directories and 
# set up output data directory
# output data dir is referenced as $OUTD

source ../bps.config

OUTD="$BPS_DATA/I_FilterDiscordant/dat"
#ln -sf $OUTD
mkdir -p $OUTD

STEPS=" 1_filter_discordant_reads.sh 2_make_Discordant_BPC.sh "
